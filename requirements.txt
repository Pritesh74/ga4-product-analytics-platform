# ── BigQuery / data access ────────────────────────────────────────────────────
# All three are required: bigquery for queries, bigquery-storage for fast Arrow
# transfers, pyarrow for columnar serialisation, db-dtypes for BQ type mapping.
google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage==2.25.0
pyarrow==16.1.0
db-dtypes==1.3.0

# ── Data / statistics ─────────────────────────────────────────────────────────
# numpy pinned to 1.x — scipy 1.13 and statsmodels 0.14 are not yet numpy-2 safe.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
statsmodels==0.14.2

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit==1.36.0
plotly==5.22.0
altair==5.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1
rich==13.7.1

# ── Data pipeline (dbt) ───────────────────────────────────────────────────────
# Used locally to build ga4_staging / ga4_marts / ga4_experiments in BigQuery.
# Not required by the Streamlit app at runtime, but included here so a single
# `pip install -r requirements.txt` covers both dashboard and pipeline work.
dbt-core==1.8.7
dbt-bigquery==1.8.2
